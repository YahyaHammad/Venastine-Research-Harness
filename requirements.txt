# The Python floor is 3.11 (mcp_client/client.py uses asyncio.timeout).
# requirements.txt cannot express that; pyproject.toml's requires-python
# can, and reads THIS file for the list below via dynamic dependencies --
# so there is one dependency list, not two. See issue #144.
#
# markitdown and protobuf moved to pyproject's `documents` extra: their
# only reachable consumer is behind read/write/edit, which are globally
# denied. `pip install -e ".[documents]"` brings them back.

anthropic==0.116.0
ddgs==9.14.4
httpx==0.28.1
openai==2.45.0
# NOTE (ROADMAP_v2 §16): this pin has no ThinkingConfig.thinking_budget --
# that field arrived in a later release. core/client.py checks for it at
# runtime and reports no effort levels on GOOGLE rather than sending a
# field the SDK cannot carry. Moving this pin enables Google effort; it
# also puts §9's verified Google implementation back in scope for retest.
google-genai==1.0.0
pydantic==2.13.4
sympy==1.13.3
sqlmodel==0.0.39
python-dotenv==1.0.1
matplotlib==3.10.3
pyyaml==6.0.3
# ROADMAP_v2 §16 TUI. Upper-bounded per D22 -- the mcp lesson was that an
# unpinned dependency mid-major-version-transition migrates you by accident.
textual>=1.0,<2.0

# Imported DIRECTLY at tui/widgets.py:8-9 (`rich.syntax.Syntax`,
# `rich.text.Text`) and previously undeclared -- present only because
# textual, mcp and httpx all happen to require it, so its major version was
# chosen by whatever textual's own range resolved to. Upper-bounded per D22
# for D22's own reason, and this was the one package the project imported
# without declaring, i.e. the one place such a migration would be silent.
rich>=13,<16

# ROADMAP_v2 §17 MCP client. Upper-bounded per D22 -- and D22 exists
# BECAUSE of this package: it was mid-major-version transition when §17
# was written, and 2.0.0 landed 2026-07-28 before implementation started.
#
# v2 is a real break for consumers, verified in hand rather than from the
# docs: result fields are snake_case (.content/.structured_content/
# .is_error, was camelCase), timeouts are float seconds (was timedelta),
# the exception is MCPError (was McpError), streamablehttp_client is
# removed, and httpx/httpx-sse are replaced by httpx2 -- which is why
# there are now two HTTP stacks installed. httpx stays; anthropic, openai
# and ddgs all still need it.
# EXACT, not the `>=2.0,<3` range this used to carry (batch 32, CI run
# 32778904550). D22's upper bound did its job -- no major migration -- but a
# MINOR still changed observable behaviour underneath it: 2.1.0 stopped
# passing a crashed tool's exception text back to the client ("the
# exception's own text stays on the server"), so a test asserting the
# message went red on main while every local run stayed green. The range
# meant CI's result depended on WHEN it ran, and CI was not testing what
# anyone runs locally -- which is the more serious half of that.
#
# So this is pinned the way anthropic, openai and google-genai already are.
# Moving it is now a deliberate step: read 2.1.0's split first -- a tool
# raising ToolError on purpose keeps its message, a tool that CRASHES has
# its text withheld -- and re-run tests/test_mcp_client.py, which is written
# to hold on both versions.
mcp==2.0.0

# --- Test dependencies -------------------------------------------------
# Previously absent, which the docs recorded as a known open item since
# §14: the suite has always needed pytest and pytest-mock, and §16's TUI
# pilot tests add pytest-asyncio. Listed now so `pip install -r` produces
# a working suite instead of one that fails at import.
pytest>=8.0
pytest-mock>=3.14
pytest-asyncio>=1.0
